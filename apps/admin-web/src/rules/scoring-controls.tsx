import { useId, type ReactNode } from 'react';
import type { RuleRange } from '@niq-scoring/contracts/rules';
import { Input } from '../components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../components/ui/select';
export function ScoringField({label,children}: {label:string;children:ReactNode}) { return <div className="space-y-2"><div className="text-sm font-medium">{label}</div>{children}</div>; }
export function ScoringSelect({label,value,options,onChange}: {label:string;value:string;options:{value:string;label:string}[];onChange:(value:string)=>void}) { return <ScoringField label={label}><Select value={value} onValueChange={onChange}><SelectTrigger aria-label={label}><SelectValue placeholder="Select…" /></SelectTrigger><SelectContent>{options.map(option=><SelectItem key={option.value} value={option.value}>{option.label}</SelectItem>)}</SelectContent></Select></ScoringField>; }
export function ScoringNumber({label,value,onChange,min,max}: {label:string;value:number|null;onChange:(value:number|null)=>void;min?:number;max?:number}) { const id=useId(); return <div className="space-y-2"><label htmlFor={id} className="text-sm font-medium">{label}</label><Input id={id} type="number" step="any" min={min} max={max} value={value??''} onChange={event=>onChange(event.target.value===''?null:Number(event.target.value))}/></div>; }
export function RangeFields({value,onChange}: {value:RuleRange;onChange:(value:RuleRange)=>void}) { return <div className="grid gap-3 sm:grid-cols-2"><div className="space-y-2"><ScoringNumber label="Minimum (blank = unbounded)" value={value.min} onChange={min=>onChange({...value,min})}/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={value.minInclusive} onChange={e=>onChange({...value,minInclusive:e.target.checked})}/>Include minimum</label></div><div className="space-y-2"><ScoringNumber label="Maximum (blank = unbounded)" value={value.max} onChange={max=>onChange({...value,max})}/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={value.maxInclusive} onChange={e=>onChange({...value,maxInclusive:e.target.checked})}/>Include maximum</label></div></div>; }
